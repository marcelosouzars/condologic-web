name: web_admin
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  url_launcher: ^6.2.6
  google_fonts: ^8.0.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.4
  excel: ^4.0.6
  csv: ^5.0.2  # <--- COLOCAMOS O CSV DE VOLTA AQUI!
  pdf: ^3.11.3
  printing: ^5.14.2
  intl: ^0.20.2
  universal_html: ^2.3.0
  file_picker: ^8.0.0
  fl_chart: ^0.65.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    # - assets/images/logo_condologic.png
    - assets/images/fundo_web.png